{{#unless ghost}}
	<a href="/image/{{imageId}}" class="image-preview gallery__image-preview" style="background-image: url('/{{previewUrl}}')">
{{else}}
	<a class="image-preview gallery__image-preview image-preview_ghost">
{{/unless}}
	<div class="image-preview__info-board">
		<span class="image-preview__text">{{comments}} comments {{likes}} likes</span>
	</div>
</a>