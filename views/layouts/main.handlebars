<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	{{#if page}}
		<title>{{page}}</title>
		<link rel="stylesheet" href="/{{page}}.css">
	{{/if}}
	{{{_sections.scripts}}}
</head>
<body{{#if loggedUser}} data-is-logged-user{{/if}}>
	{{{body}}}
</body>
</html>
