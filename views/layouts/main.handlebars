<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	{{#if page}}
		<title>{{page}}</title>
		<link rel="stylesheet" href="/{{page}}.css">
	{{/if}}
	<script>
		window.isLogged={{#if loggedUser}}true{{else}}false{{/if}};
	</script>
	{{{_sections.scripts}}}
</head>
<body>
	{{{body}}}
</body>
</html>
